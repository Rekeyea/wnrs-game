export enum GameLevel {
  PERCEPTION = 1,
  CONNECTION = 2,
  REFLECTION = 3
}

export enum GameStatus {
  SETUP = 'setup',
  IN_PROGRESS = 'in_progress',
  LEVEL_COMPLETE = 'level_complete',
  GAME_OVER = 'game_over',
  PAUSED = 'paused'
}